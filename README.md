# my_own_nm

Утилита nm написанная в учебных целях (для знакомства с устройством бинарных файлов). Достает из бинарного файла таблицу символов. Работает с форматом ELF64. Пока что не поддерживает статические библиотеки (например libft.a). Поддерживает опцию -n для сортировки по адресам.
